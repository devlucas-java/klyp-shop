global:
  scrape_interval: 15s

  scrape_configs:
    - job_name: 'otel-collector'
      static_configs:
        -targets: [`otel-collector:8080`]

    - job_name: 'klyp-shop-db'
      static_configs:
        -targets: [`host.docker.internal:5433`]