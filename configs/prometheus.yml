global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:

  - job_name: "klyp-shop"
    static_configs:
      - targets: ["app:8080"]
    metrics_path: /metrics

  - job_name: "postgres"
    static_configs:
      - targets: ["postgres-exporter:9187"]
